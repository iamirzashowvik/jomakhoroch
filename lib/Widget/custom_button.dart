import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class CustomButton {
  Widget customBtn(String title) {
    return ElevatedButton(
      onPressed: () {},
      child: Text(title),
    );
  }
}
